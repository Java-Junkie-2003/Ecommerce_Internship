const { BadRequestError, NotFoundError } = require("../core/error.response");
const {findUserByPhoneNumber} = require('../models/repositories/user.repo')
class UserService {
    static updateInfo = async({phone, })
}